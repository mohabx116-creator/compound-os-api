import { z } from 'zod';

// TODO: Define Unit Zod validation schemas here

export const createUnitSchema = z.object({});
export const updateUnitSchema = z.object({});
export const unitQuerySchema = z.object({});
export const unitParamsSchema = z.object({});
