import { z } from 'zod';

// TODO: Define Compound Zod validation schemas here

export const createCompoundSchema = z.object({});
export const updateCompoundSchema = z.object({});
export const compoundQuerySchema = z.object({});
export const compoundParamsSchema = z.object({});
